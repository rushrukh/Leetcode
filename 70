class Solution {
public:
    int climbStairs(int n) {
        int first = 0, second = 1;
        
        for(int i = 1; i <= n; i++) {
            int tmp = second + first;
            first = second, second = tmp;
        }
        
        return second;
    }
};
